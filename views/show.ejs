<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Note</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <script defer src="/scripts/theme.js"></script> 
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body class="bg-white text-black dark:bg-zinc-900 dark:text-white transition-colors duration-300">

    <div class="w-full min-h-screen flex flex-col items-center justify-center p-6">
        
        <!-- Back Button -->
        <a class="text-blue-500 dark:text-blue-400 hover:underline absolute top-5 left-5" href="/">⬅ Go back to home</a>

        <!-- Note Container -->
        <div class="w-full max-w-2xl bg-gray-100 dark:bg-zinc-800 p-8 rounded-lg shadow-lg transition-transform transform hover:scale-105">
            <h1 class="text-4xl font-bold text-blue-500 dark:text-blue-400 tracking-tighter"><%= filename %></h1>
            <p class="text-lg mt-5 font-light text-gray-700 dark:text-gray-300 leading-relaxed"><%= filedata %></p>
        </div>
    </div>

</body>
</html>
 v